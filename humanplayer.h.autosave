#ifndef HUMANPLAYER_H
#define HUMANPLAYER_H

#include "player.h"

class HumanPlayer: public Player {
    
public:
    HumanPlayer();
    void setArenaSize(QSize arenaSize);
    void setPlayerSize(QSize playerSize);
};

#endif // HUMANPLAYER_H
